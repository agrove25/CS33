#include <time.h>
#include <stdlib.h>
#include <stdio.h>

int main(NULL) {
  srand(time(NULL));
  
  int x = rand();
  int y = rand();
  int z = rand();

  double dx = (double)x;
  double dy = (double)y;
  double dz = (double)z;

  printf("%i\n", x);
}
